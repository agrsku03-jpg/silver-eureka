import { customer } from '@/lib/mockData';import { PageHeader } from '@/components/Layout';export default function Page(){return <><PageHeader title="Profile & security" subtitle="Manage personal details, security preferences, and alerts."/><div className="mx-auto grid max-w-7xl gap-6 px-4 py-10 md:grid-cols-2"><div className="card"><h2 className="text-2xl font-bold">Profile</h2><p>{customer.name}</p><p>{customer.email}</p><p>{customer.tier}</p></div><div className="card space-y-3"><h2 className="text-2xl font-bold">Security settings</h2>{['Two-factor authentication enabled','Login alerts on','Paperless statements on','Device trust review'].map(x=><label className="flex justify-between" key={x}>{x}<input type="checkbox" defaultChecked/></label>)}</div></div></>}
